# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_REGION=us-east-1
# 若使用 SSO / STS 暫時憑證，需要額外設定 session token
AWS_SESSION_TOKEN=

# Bedrock Mantle API key（bearer token，僅限 Bedrock / Bedrock Runtime，優先權高於上方 SigV4 憑證）
AWS_BEARER_TOKEN_BEDROCK=
