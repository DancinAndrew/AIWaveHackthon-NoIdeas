# macOS
.DS_Store

# Secrets and local environment
.env
.env.*
!.env.example

# Python and uv
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
build/
dist/

# Lambda packaging
package/
lambda_deploy.zip
# Local scratch files
tmp/
_tmp_*

# Node dependencies, wherever they get installed. Previously only the three
# known subdirectories were listed, so a root-level install was committed.
node_modules/

# Frontend
frontend/node_modules/
frontend/package-lock.json

# AWS CDK
cdk.context.json
infra/node_modules/
infra/cdk.out/
.cdk.staging/

# packages/backend (reference)
packages/backend/node_modules/
